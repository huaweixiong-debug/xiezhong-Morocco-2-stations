# ATEQ 双工位气密检测系统 · 项目说明

## 1. 当前状态（2026-09-11）

本项目是从 LabVIEW `Main.vi` 迁移的 Python/PySide6 双工位上位机，运行电脑为
`100.87.176.32`，生产数据目录为 `D:\data`。

- PLC：`192.168.2.1:102`，点位以确认后的 `PLC通讯点位表.xlsx` 为准：A/B 启动
  `M16.0/M16.1`，A/B 扫码 OK `M0.1/M0.0`，A/B 手动切换 `M2.0/M2.1`。
- 已确认的主要点位：

  | 信号 | A 工位 | B 工位 |
  |---|---:|---:|
  | 安全门禁用 | `M0.6` | `M0.7` |
  | 复位 | `M0.3` | `M0.2` |
  | NG/OK 样件需求 | `M1.2` / `M1.3` | `M1.4` / `M1.5` |
  | 校准到期 | `M1.0` | `M1.1` |
  | 启动 | `M16.0` | `M16.1` |
  | 手动切换 | `M2.0` | `M2.1` |
  | 扫码 OK | `M0.1` | `M0.0` |
  | 封堵/盖章 | `M4.2` / `M4.6` | `M3.2` / `M3.6` |
  | 夹紧/移载 | `M4.4` / `M4.0` | `M3.4` / `M3.0` |
  | 正负压开启 | `M0.5` | `M0.4` |
- 共用扫码枪：`192.168.2.10:9004`。测试页建立单一 TCP 在线接收通道，扫码帧进入后
  自动按 A/B 二维码精确路由；操作员不选择工位，断线时后台自动重连并在状态栏提示。
  测试页只有一个扫码枪状态灯和“开启扫码/关闭扫码”测试按钮；按钮仅控制输入接收，
  不与 A/B 测试程序关联；开启时向扫码枪发送 `LON\r\n`，关闭时发送 `LOFF\r\n`，
  TCP 重连后按当前状态自动重发。标签重打按钮分别属于 A/B 工位。
- 打印：BarTender 11 + `ZDesigner ZT231-203dpi EPL`，A/B 共用一台打印机。
- ATEQ：B 工位 F620 已在远程机只读确认 `COM6 / 从站 1`，连续实时块读取稳定，当前
  程序号读回为 1；COM4 在远程机不存在。A 工位仍未确认，故 A/B 完整映射和
  `ports_confirmed` 仍保持阻断，`D:\data\Setup.ini` 暂不改为生产确认状态。
- 数据库：目标为本机 MySQL 8.4 LTS，库 `test`，分表 `info_A`、`info_B`。
- 运行门禁：当前默认 `simulate`。真实 ATEQ、MySQL、模板复核全部通过前，LIVE 预检失败。
- B 工位单机通讯验证：运行 `run_b_test.bat`，仅打开 `COM6 / 从站 1`，只读实时寄存器块和当前
  程序号，不连接 A 工位、PLC、扫码枪、数据库或打印机；如需写入程序号，必须显式追加
  `--b-program N`，验证完成后自动关闭串口。
- `run_ui.bat` / `launch_ui.ps1` 当前启动“B 真实联机 UI”模式：B 卡使用
  `SerialAteq(COM6, slave=1)`，PLC `192.168.2.1:102`、本机 MySQL `127.0.0.1:3306`、
  共用扫码枪和 BarTender/打印机均使用真实设备；PLC、数据库、ATEQ、扫码枪或打印机任一项
  不可用时启动阻断，不会回退到模拟。A 卡不接真实仪器。默认纯模拟 UI 仍可直接执行
  `D:\Python310\python.exe -m app.main --mode simulate`。

原 LabVIEW 工程不修改。代码、配置和模板变更前均需时间戳备份。

## 2. 正确关联关系

### 2.1 A/B 独立选择和周期冻结

测试页 A、B 工位各自选择产品型号、人员及单测/双测，互不覆盖。周期扫码成功时冻结：

- 工位
- 产品型号和客户型号
- 人员
- 单测/双测
- 流水号与二维码
- ATEQ 程序号
- 该工位模板路径

周期完成或复位前，不再读取界面上的后续改动。

设置页每个型号只维护一个 `ATEQ程序号`（1–255）和一个逻辑 `打印模板` 族；运行时派生：

```text
D:\data\<型号>-A.btw
D:\data\<型号>-B.btw
```

发送给 ATEQ 的寄存器值是 `程序号 - 1`，写入后必须读回验证。

设置页显示和编辑时，条码规则列加宽；日期方案用 `YYYYMMDD`、`Y2-M1-D2` 等无中文显示码，
保存时仍写回原始日期方案。ATEQ 程序号为 1–255 可编辑数值框。打印模板分别显示并保存为
`打印模板 A`、`打印模板 B`；人员列表单独收窄，提供“新建人员 / 删除人员 / 保存人员”。

### 2.2 二维码生成

`app/barcode_rules.py` 参考旧 `Data\Barcode.py`，并采用 fail-closed 写法：

1. 从该工位 `序列号A.txt` 或 `序列号B.txt` 读取流水号，不递增、不把扫码值当流水号。
2. 从 `日期设置.ini` 读取客户型号、条码规则、日期方案、工位号、程序和模板族。
3. 从 `日期对照.ini` 生成日期码。
4. 生成后同步写入 `二维码A/B.txt`、`打印路径A/B.txt`、`协众产品号A/B.txt`。
5. 上述 TXT 使用 UTF-8 无 BOM、无尾随换行，并用临时文件原子替换。

配置、日期、流水号或模板缺失时禁止开始周期。

固定回归：

```text
E122015400，2025-12-30，A，0082 => E12201540025123080082
E122015400，2025-12-30，B，0056 => E12201540025123090056
```

### 2.3 共用扫码枪自动路由

扫码枪收到条码后与当前 A/B 预生成二维码做精确比较：

- 只匹配 A：进入 A。
- 只匹配 B：进入 B。
- 都不匹配、同时匹配、目标工位忙或重复扫码：拒绝并报警。

生产页不依赖“扫码到 A/B”人工按钮；旧按钮仅保留为 SIMULATE 管理员诊断兼容入口。

匹配成功后上位机只置位一次对应扫码 OK 位：A=`M0.1`、B=`M0.0`，等待 PLC 自行清零；
上位机不抢先清零。5 秒未清零，该工位进入故障。PLC 访问由同一把锁串行化，位写入执行
读改写，避免 A/B 位处于同一字节时互相覆盖。生产启动信号为 A=`M16.0`、B=`M16.1`，
手动切换信号为 A=`M2.0`、B=`M2.1`。启动信号的所有权属于 PLC：Python 只读
`M16.0/M16.1` 的 0→1 上升沿，不写入、不清除启动位。程序启动后的第一次采样只建立
基准电平，避免把已经保持为 1 的旧信号误当成新命令。

每个工位的 PLC→ATEQ 链路独立运行：A 工位上升沿只调用 A 控制器，B 工位上升沿只调用
B 控制器；两条链路使用独立工作线程，不会因另一台 ATEQ 正在等待响应而互相阻塞。扫码成功
后工位进入“就绪”，第一次上升沿执行一测；双测一测为 OK 后进入“等待第二次测试”，
下一次该工位自己的上升沿才执行二测。单测或 NG 结束后，新的上升沿不会跳过扫码直接开新
周期，而是拒绝并使该工位进入故障，必须重新扫码建立周期。

启动顺序必须保持为：PLC 置位 `M16.0/M16.1` → 对应 UI 检测到 0→1 上升沿 → UI 通过
对应 ATEQ 的 Modbus RTU 写单线圈 `0x0001=ON` 启动仪器 → UI 读取实时块并等待
`StepCode 4/5/6`（Fill/Stabilization/Test）→ 读取完整结果后再落库、打印。UI 不写入或
清除 `M16.0/M16.1`；启动线圈命令失败或周期未进入有效 StepCode 时禁止落库和打印。

## 3. ATEQ F620

`app/ateq.py` 为 A/B 提供独立串口对象和独立锁：

- 默认 `9600 / 8 / Even / 1`。
- 从站地址可配置；接机时只读尝试 `1`、`255`，成功后固化。
- 程序号写入寄存器 `0x0200`，值为 `N-1` 并字节交换，随后读取实时块校验。
- 实时读取保持寄存器 `0x0030`、13 个寄存器。
- 启动命令发出后必须观察到有效周期步骤 `StepCode 4/5/6`；结束帧兼容 `65525/65535`，
  并同时校验周期结束位、FIFO 序列和 OK/NG 位。
- 周期结束位、FIFO 序列、OK/NG 位、CRC 和请求身份必须一致。
- 压力、泄漏值按仪器原始单位数值保存；接机验收时再与屏幕逐项核对单位。

程序不匹配、CRC 错误、串口掉线、结果不明确、重复 FIFO 或周期身份不一致时，禁止落库和打印。

校准标签或生产标签收到 BarTender 成功回执后，UI 立即对共用扫码枪发送 `LON`；现场 SR-750
数据端口 1 的 ASCII 结束符为 `CR`，程序必须使用单个 `\r`（不能使用 `CRLF`）。若扫码枪
暂时离线，命令先排队并在 TCP 重连时重放。打印失败不会发送 `LON`。

## 4. MySQL 分表

本机数据库固定为 `test`，A/B 分别写：

```text
test.info_A
test.info_B
```

两表同结构，保留旧 LabVIEW 字段：

```text
Time, Serial No., 2D Code,
1 Pressure, 1 Leakage, 2 Pressure, 2 Leakage,
Result, Part No., Person, labeled
```

新增 `id BIGINT AUTO_INCREMENT PRIMARY KEY`、唯一 `cycle_id`，并为时间、二维码和流水号建索引。
存储引擎为 InnoDB，字符集为 utf8mb4。

- 单测一测结束、双测二测结束或任一 NG 后，才写完整记录，初始 `labeled=0`。
- 只有 OK 才打印。
- BarTender 返回成功后才把同一 `cycle_id` 更新为 `labeled=1`。
- NG、结果不明确或打印失败保留记录，但不标记已打印。
- A 周期只能写 `info_A`，B 周期只能写 `info_B`。

安装文件：

- `tools/mysql_schema.sql`
- `tools/install_mysql84.ps1`

安装脚本校验官方 MySQL 8.4.11 MSI 的 MD5，数据目录放在 `D:\MySQLData`，服务自动启动，
仅监听 `127.0.0.1:3306`。应用账号只有两张表的 `SELECT/INSERT/UPDATE` 权限；凭据保存在
`C:\ProgramData\LeakTest2Channels\db.json` 并限制 ACL，不写入源码和日志。

## 5. 打印与模板

打印事务由全局互斥锁覆盖完整的“写全部 TXT → 调用 BarTender → 读取退出回执”，防止 A/B 并发串站。
`客户产品号A/B.txt` 写客户型号，不再误写产品型号。

生产模板：`<型号>-A.btw`、`<型号>-B.btw`。
校准模板：`Cal_NG_A/B.btw`、`Cal_OK_A/B.btw`。

`EV80016300-B.btw` 已从 A 版安全复制，但必须在远程 BarTender 中把所有 A 文件数据源改绑为 B，
打印目检通过后创建 `D:\data\EV80016300-B.rebind-ok`。缺少此复核标记时 LIVE 预检必定失败。

## 6. 校准隔离

A/B 各自拥有校准状态和锁：软件启动时两工位先显示“校准到期”，只能先点击对应工位的
“启动验证”，完成 NG→OK 后才允许该工位正常扫码；某工位后续计时到期时只锁该工位，
另一工位继续生产。设置页的 `校准周期`（例如 `02:00:00`）会在下一次正常扫码后按工位
独立倒计时，倒计时显示为 `HH:MM`，归零自动重新点亮校准到期灯。

```text
校准到期灯亮 -> 当前周期完成 -> 点击“启动验证”
-> NG 首件验证符合预期（NG 灯亮）
-> OK 二件验证符合预期（OK 灯亮）
-> 允许下一周期扫码 -> 三个状态灯全部熄灭
```

“校准到期”“NG 首件”“OK 二件”是只读状态指示灯，只有“启动验证”是按钮；
启动验证不会写生产启动位。验证中的 A/B 状态相互独立，顺序或结果错误保持锁定。
NG/OK 验证完成后，三灯先保持亮起供操作员确认，下一次扫码成功时原子清除。
取消校准只允许管理员，并记录账号、原因、时间和工位。

## 7. 运行和验收

```powershell
# 模拟 UI
D:\Python310\python.exe -m app.main --mode simulate

# 模拟双工位回归
D:\Python310\python.exe -m app.main --mode simulate --smoke-cycle

# 自动测试
D:\Python310\python.exe -m pytest tests -q

# 真实条件只读预检（失败不会启用生产输出）
D:\Python310\python.exe -m app.main --config config\live.toml --mode live --preflight
```

LIVE 必须同时通过：MySQL、PLC、扫码枪、两台 ATEQ、BarTender、打印机、日期配置、序列号、
当前型号全部模板以及 EV800 B 数据源人工复核。之后仍需完成：

1. ATEQ 逐台端口/从站/程序读回与屏幕结果验收。
2. 真实扫码只触发对应扫码 OK 位 `M0.1/M0.0`，并验证 PLC 清零；启动联动核对
   `M16.0/M16.1`。
3. A/B 选不同型号同时生产，确认数据库严格分表。
4. 所有实际 `.btw` 每个打印一张并核对字段。
5. 100 周期 shadow 对照、A/B 并发生产、8 小时稳定运行。
6. 数据库备份恢复和现场安全确认。

任何一项未完成，都不得将 `ports_confirmed` 改为 `true`，也不得启用 LIVE 输出。
