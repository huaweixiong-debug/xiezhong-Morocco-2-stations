# ATEQ 双工位关联修复 · 实施验收记录

记录日期：2026-09-11

## 已完成

- A/B 独立型号、人员、单测/双测及不可变周期快照。
- 流水号与扫码二维码分离。
- 兼容旧 `Barcode.py` 的二维码生成，固定回归已覆盖。
- 共用扫码枪按 A/B 当前二维码自动路由，拒绝未匹配、双匹配、忙碌和重复扫码。
- 测试页已接入单一在线 TCP 扫码枪 `192.168.2.10:9004`；收到扫码帧后自动比较 A/B
  二维码并路由，不再让操作员选择“扫码到 A/B”，断线自动重连并显示状态。
- 测试页只保留一个共用扫码枪状态和“关闭扫码/开启扫码”测试按钮；按钮只控制扫码输入
  是否接收，不启动或改变测试程序；开启发送 `LON\r\n`，关闭发送 `LOFF\r\n`，重连后按当前
  状态自动重发。已移除工位卡片上的重复扫码状态和看门狗控件，标签重打按 A/B 工位分别显示。
- 测试页校准脚区固定为 4 个项目：校准到期、启动验证、NG 首件、OK 二件；前三项为放大的
  状态灯，启动验证为同尺寸按钮，移除原“扫码”提示项。
- PLC 已按确认点位表修正：A/B 启动 `M16.0/M16.1`、扫码 OK `M0.1/M0.0`、
  手动切换 `M2.0/M2.1`；共享锁、位级读改写、置位后等待 PLC 清零、5 秒超时故障。
- 已实现 PLC 启动边沿联动：Python 只读 `M16.0/M16.1` 的 0→1，上升沿分别调度 A/B
  控制器；第一次边沿做一测，双测等待下一次本工位边沿做二测，启动位不由 Python 写入或清除。
- 两台 ATEQ 独立串口/锁；9600/8/E/1、从站可配、1/255 只读探测、程序 `N-1`
  写入读回、0x30 实时块、CRC、`65535→4` 新周期、FIFO/结束/OK-NG 校验。
- MySQL `test.info_A/info_B` DDL 和幂等适配器；最终记录先 `labeled=0`，打印成功后更新为 1。
- 打印全局互斥事务；客户产品号字段改为客户型号。
- A/B 校准状态隔离，校准模板按工位命名，管理员取消需原因并审计。
- 校准页脚已按现场流程修正：校准到期、NG 首件、OK 二件为只读状态灯；仅“启动验证”为按钮。
  软件启动时两工位先点亮“校准到期”；点击对应“启动验证”后必须按 NG→OK 顺序验证。
  NG 首件验证成功先打印 `Cal_NG_A/B.btw`，OK 二件验证成功再打印 `Cal_OK_A/B.btw`；任一打印
  未获得回执都不点亮对应完成状态，也不解除校准锁定。
  启动验证不写 PLC 生产启动位，NG→OK 成功后三灯保持可见，下一次扫码成功才一起熄灭；
  下一次正常扫码启动该工位的独立校准倒计时，周期归零自动重新锁定该工位。
- 设置页已调整为宽条码规则、无中文日期方案显示、可编辑 ATEQ 程序号、A/B 独立打印模板，
  以及收窄的人员列表和新建/删除/保存操作；校准倒计时改为 A/B 两个独立控件，指示灯和文字统一放大。
- `simulate/characterization/live` 配置和 LIVE 只读预检；未满足条件不会启用生产 UI。
- `config/fieldtest.toml` 中损坏的 CR/LF 和 BarTender 路径控制字符已修复。
- 远程 `D:\data` 型号配置已迁移；日期对照、四个校准模板、EV800 B 安全副本已部署。
- 启动流程已改为两工位默认先显示“校准到期”：点击对应“启动验证”后按 NG→OK 顺序验证，
  完成验证后的第一件正常扫码才清除三灯并启动该工位独立倒计时。设置页 `02:00:00` 已按秒
  生效，倒计时以 `HH:MM` 显示，归零自动重新锁定对应工位。

## 自动验证

```text
76 passed
Python compileall: PASS
install_mysql84.ps1 parser: PASS
MySQL 8.4.11 MSI MD5: b5c515a0f410cd6903cd41057ed5d662（与官方一致）
```

## 当前 LIVE 阻断项

1. B 工位 ATEQ 已只读确认 `COM6 / 从站 1`，连续实时块读取稳定；COM4 在远程机不存在。
   A 工位仍未确认，`D:\data\Setup.ini` 继续保持 `UNCONFIRMED`，不提前开启 LIVE。
2. 远程 Windows 当前只授权 `D:` 共享盘修改；WMI、远程计划任务、服务创建和 SSH 均拒绝执行，
   所以 MySQL 服务尚未注册。安装包和管理员脚本已放到远程机。
3. `EV80016300-B.btw` 当前与 A 版 SHA256 相同，说明只是副本，尚未在 BarTender 中重绑 B 数据源；
   `D:\data\EV80016300-B.rebind-ok` 不存在，LIVE 预检会阻断。
4. 真实 ATEQ 帧、真实打印回执、100 周期 shadow、A/B 并发和 8 小时稳定运行尚未验收。

## 远程机管理员必须执行一次

在 `100.87.176.32` 上以管理员身份打开 PowerShell：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\ATEQ\tools\install_mysql84.ps1
```

脚本会校验 MSI、安装 MySQL、创建 `test.info_A/info_B`、创建最小权限应用账号、限制凭据 ACL，
且不输出密码。执行结束后再运行：

```powershell
cd D:\ATEQ
D:\Python310\python.exe -m app.main --config config\live.toml --mode live --preflight
```

## ATEQ 接机步骤

1. 只连接一台 ATEQ，分别对 COM4/COM6 用只读探测确认端口与从站 1/255。
2. 在屏幕确认它属于 A 或 B，再写 `Setup.ini` 和 `config/live.toml`。
3. 选择一个已知程序，验证写入值 `N-1`、读回程序号、屏幕程序号三者一致。
4. 做 OK/NG 实测，逐项对照压力、泄漏、单位、结束位和结果。
5. 第二台重复上述步骤；两台都确认后才把 `ports_confirmed=true`。

## EV80016300-B 重绑步骤

在远程 BarTender 打开 `D:\data\EV80016300-B.btw`，把所有 A 工位 TXT 数据源改绑到 B：

```text
二维码A.txt       -> 二维码B.txt
序列号A.txt       -> 序列号B.txt
客户产品号A.txt   -> 客户产品号B.txt
正压值A.txt       -> 正压值B.txt
负压值A.txt       -> 负压值B.txt
打印路径A.txt     -> 打印路径B.txt
```

保存后打印一张核对所有字段。确认通过后创建空标记文件：

```powershell
New-Item -ItemType File -Path D:\data\EV80016300-B.rebind-ok -Force
```

未完成打印目检时禁止创建标记。
